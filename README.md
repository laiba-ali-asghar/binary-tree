# binary-tree
This project demonstrates a basic implementation of a Binary Tree using an array in C++. Instead of using dynamic memory allocation and pointers (as in linked structures), this approach stores tree elements sequentially in an array, where the index relationships determine parent-child links
